node_modules/
# Recorded output. These are large binaries regenerated on every run, and the
# raw/ webm files are intermediates ffmpeg consumes.
demo_videos/
.env
.DS_Store

# Written by scripts/stage-data.js while the load-test fixture is hidden.
.staged-listings.json
